name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  validate-agents:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate agent definitions
        run: make validate-agents